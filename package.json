{
  "name": "localized-entity",
  "version": "1.0.3",
  "description": "A vanilla JS middleware used to abstract away the bidirectional data-binding of localized object model attributes.",
  "main": "src/index.js",
  "type": "module",
  "scripts": {
    "test": "vitest --coverage --run --reporter=verbose",
    "devtest": "vitest --coverage",
    "example": "browser-sync start --server --files \"src/**/*.js\""
  },
  "author": "Justin Lam",
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "https://github.com/flybits/localized-entity.git"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^2.1.8",
    "browser-sync": "^3.0.3",
    "vite": "^6.0.7",
    "vitest": "^2.1.8"
  }
}
